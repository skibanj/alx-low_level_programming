#include "main.h"

/**
 * more_numbers - A function that prints more numbers
 * Return: Always 0 (Success)
 */

void more_numbers(void)
{
	int i, j;

	for (j = 0; j <= 9; i++)
	{
		for (i = 0; i <= 14; i++)
		{
		_putchar(i);
		}
		_putchar('\n');
	}
}
